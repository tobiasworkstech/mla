version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - CLAUDE_API_KEY=${CLAUDE_API_KEY:-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}
    volumes:
      - ./backend/.env:/app/backend/.env:ro
      - /Users:/Users:ro
      - /tmp:/tmp:ro
    container_name: memory-leak-analyzer-backend
    restart: unless-stopped
    networks:
      - analyzer-network

  frontend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://backend:4000
    command: sh -c "npm install && cd frontend && npm run dev -- --host 0.0.0.0 --port 3000"
    container_name: memory-leak-analyzer-frontend
    restart: unless-stopped
    networks:
      - analyzer-network

networks:
  analyzer-network:
    driver: bridge
